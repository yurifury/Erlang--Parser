#!/usr/bin/env perl -w

use strict;
use warnings;
use 5.014;

use Test::Simple tests => 1;

# TODO: test all the various call syntaxes
# x(), X(), (X())(), ((X())())(), X:Y(), 'x':'y'(), (x()):Y(), ?X:?Y() ...
